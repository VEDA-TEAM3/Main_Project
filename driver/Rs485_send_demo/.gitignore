# ---------------------------------------------------------------------------
# STM32CubeIDE 빌드 산출물. 전부 재생성되므로 추적하지 않는다.
# (Debug/ 안에는 .o/.d/.su/.cyclo 와 CubeIDE가 만드는 makefile 이 함께 들어있다)
# ---------------------------------------------------------------------------
/Debug/
/Release/

# ---------------------------------------------------------------------------
# 호스트 테스트 실행 파일. tools/*.c 만 추적하고 빌드 결과는 무시한다.
#   gcc -Wall -Wextra -I../Core/Inc test_sched_loop.c -o test_sched_loop
# ---------------------------------------------------------------------------
/tools/*.exe
/tools/test_sched_select
/tools/test_sched_loop

# ---------------------------------------------------------------------------
# 컴파일 부산물 (어느 경로에 생기든)
# ---------------------------------------------------------------------------
*.o
*.d
*.su
*.cyclo
*.map
*.list
*.elf
*.bin
*.hex

# ---------------------------------------------------------------------------
# 편집기 / OS
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
*~

# 뮤테이션 검사 작업 폴더 (실행 중에만 존재)
/tools/.muttmp/
/tools/_mut_*.exe
